import { marketingSeo } from "@/lib/marketing-seo";

export const metadata = marketingSeo.deliveryEta;

export default function DeliveryEtaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
