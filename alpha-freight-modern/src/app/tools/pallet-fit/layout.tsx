import { marketingSeo } from "@/lib/marketing-seo";

export const metadata = marketingSeo.palletFit;

export default function PalletFitLayout({ children }: { children: React.ReactNode }) {
  return children;
}
