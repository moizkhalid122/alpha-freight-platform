import { marketingSeo } from "@/lib/marketing-seo";

export const metadata = marketingSeo.fuelSurcharge;

export default function FuelSurchargeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
