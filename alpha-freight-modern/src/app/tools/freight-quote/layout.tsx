import { marketingSeo } from "@/lib/marketing-seo";

export const metadata = marketingSeo.freightQuote;

export default function FreightQuoteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
