import { marketingSeo } from "@/lib/marketing-seo";

export const metadata = marketingSeo.carrierMargin;

export default function CarrierMarginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
